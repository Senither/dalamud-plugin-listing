{
  "scripts": {
    "dev": "npx @tailwindcss/cli -i styles.css -o assets/styles.css --watch",
    "build": "npx @tailwindcss/cli -i styles.css -o assets/styles.css --minify"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "tailwindcss": "^4.2.1"
  }
}
